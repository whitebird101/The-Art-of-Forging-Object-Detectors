万物·炼器
从零打造工业级旋转目标检测网络  ——  借鉴 YOLO11 OBB 的设计之道

> 一个面向卷积神经网络与目标检测初学者的“从零手搓”系列。  
> 不满足于会调用YOLO，而是希望把一个现代目标检测模型真正拆开、看懂、写出来，并最终学会根据自己的任务需要去修改它。  
> YOLO11 OBB 是本系列后期用于深入理解目标检测的一张参考蓝图，但这个系列并不是只为“旋转目标检测”而写。

---

1. 这是一个什么样的项目？

《万物·炼器》是一个围绕 “卷积神经网络与目标检测” 展开的系列学习项目。

项目包含：

- 系列技术文章；
- 与文章同步的实验代码；
- 从简单网络到完整检测器的逐步实现；
- 对 YOLO11 OBB 关键结构与设计思想的分析；
- 从普通目标检测进一步走向 OBB 旋转目标检测的完整扩展过程；
- 学习过程中形成的实验、验证与调试记录。

旋转目标检测（OBB）会在后面的内容中占据重要位置，但它更像是这个系列最终选择的一条“进阶实战路线”，而不是整个系列唯一的主题。系列文章中的卷积、反向传播、Backbone、FPN、多尺度特征、检测头、标签分配、Loss 等内容，本质上都属于通用的卷积神经网络与目标检测知识。

这个系列不会一上来就告诉你：

```python
model = YOLO(...)
model.train(...)
```

然后模型就跑起来了。
相反，我们更希望把这些看似“黑盒”的东西一层一层拆开。

例如：

- 一层卷积到底做了什么？
- 卷积核里的参数是怎么学出来的？
- Loss 为什么能够让几十层之前的参数发生变化？
- Backbone、Neck、Head 分别负责什么？
- 为什么目标检测需要多尺度特征？
- FPN 为什么要做上采样和特征融合？
- 为什么成熟网络不是简单地不断堆叠 `Conv2d`？
- Bottleneck、C2f、C3k2、SPPF 为什么这样设计？
- DFL 到底在预测什么？
- TAL 为什么能够决定哪些预测点应该成为正样本？
- ProbIoU 为什么适合旋转框？
- 普通目标检测又是怎样一步一步变成 OBB 旋转目标检测的？

最终，希望我们不仅能够“使用一个 YOLO 模型”，而是真正知道：

> **一个现代目标检测网络究竟是怎样炼成的。**

---

## 2. 为什么要写这个系列？

最初写这些文章，其实并不是为了写教程。而是为了学习。

在学习卷积神经网络和目标检测的过程中，我经常遇到一个问题：

很多东西看资料的时候似乎“看懂了”。

卷积好像懂了，反向传播好像懂了，FPN、DFL、TAL 也似乎能看明白。

但是如果合上资料，重新问自己：

> **为什么一定要这样设计？**

或者：

> **如果不用现成框架，让我自己写出来，我会不会？**

很多时候，就说不清楚了。

于是我开始尝试费曼学习法：

> **把自己当成老师，把文章写给一个几乎不了解人工智能的人看。**

如果一个知识点不能用简单、清楚的话解释出来，那么很可能还没有真正理解它。

所以这个系列有一个很朴素的目标：

> **尽量把目标检测中的复杂概念，写成一个有 Python 基础、但几乎没有人工智能背景的人，也能够一步一步看懂的内容。**

在写作过程中，如果发现某个地方怎么解释都很别扭，往往意味着自己理解得还不够。那就重新查资料、看源码、做实验、改代码，再回来重写。

某种意义上，这些文章首先是写给我自己的学习笔记。

如果它们也能够帮助其他正在学习目标检测的人少走一些弯路，那就更有意义了。

---

## 3. 我们不只是要会用 YOLO

现在使用 YOLO 已经非常方便。

很多任务几行代码就能完成：

```python
from ultralytics import YOLO

model = YOLO("xxx.pt")
model.train(...)
```

这当然很好。

成熟框架极大降低了目标检测的使用门槛。

但如果学习止步于这里，我们往往只能回答：

> **YOLO 怎么用？**

却很难回答：

> 为什么这样设计？

> 模型为什么在这个数据集上效果不好？

> 如果目标特别小，应该改哪里？目标长宽比极端，又应该改哪里？

> 如果输入数据不是普通 RGB 图像，该怎么办？

> 如果任务需要新的检测头、新的 Loss、新的特征融合方式，该从哪里下手？

> 如果官方模型不完全适合自己的任务，能不能自己修改？

而真正进入科研、工程或特定行业应用以后，我们经常遇到的恰恰不是：

> “YOLO 会不会用？”

而是：

> **“现成的 YOLO 不完全适合我的任务，我应该怎么改？”**

因此，这个系列希望完成三个层次的跨越：

```text
会调用 YOLO
    ↓
能看懂 YOLO
    ↓
能够根据任务需要修改 YOLO
```

例如，根据实际任务，我们可能需要：

- 修改 Backbone；
- 调整特征提取结构；
- 改变 Neck 的特征融合方式；
- 增加新的尺度；
- 针对小目标优化特征图；
- 修改检测头；
- 调整正负样本分配策略；
- 替换或重新设计 Loss；
- 增加新的输入模态；
- 针对旋转目标重新设计边界框表达；
- 修改训练流程；
- 根据自己的业务需求增加新的输出。

如果只会调用 API，这些事情几乎无从下手。

所以本系列更希望培养的是：

> **读懂模型、理解模型、修改模型，最终能够自己设计模型的能力。**

---

## 4. 这个系列并不只讲 OBB

这里需要特别说明一件事：

> **这个系列不是一套专门针对旋转目标检测编写的教程。**

它首先是一套关于 **卷积神经网络和目标检测** 的学习文档。

我们从神经元、卷积、计算图和反向传播开始，再逐步进入 Backbone、FPN、多尺度特征、Detection Head、Loss、标签分配等内容。这里绝大多数知识，都是现代目标检测中的通用基础，并不只属于 OBB。

那么，为什么最终选择 **YOLO11 OBB** 作为主要参考蓝图？

### 4.1 为什么选择 OBB？

因为相比普通的水平目标检测，**OBB 是一种更加一般的目标检测形式。**

普通水平框可以看作旋转框在角度固定时的一种特殊情况。也就是说，学习 OBB 并不是离开普通目标检测，转去学习一个完全不同的问题，而是在已经掌握普通目标检测之后，再向前走一步，把“目标的方向”也纳入检测范围。

这样一来，我们在学习 OBB 的过程中，仍然会完整经历目标检测最核心、最通用的技术路线：

- 卷积与特征提取；
- Backbone；
- 多尺度特征；
- FPN / Neck；
- Detection Head；
- 边界框回归；
- DFL；
- 正负样本分配；
- Loss；
- 训练与推理。

在这些通用能力之上，再继续接触角度预测、旋转框表达、旋转目标匹配和旋转框 Loss 等问题。

所以选择 OBB，并不是为了把这个系列限定成“旋转目标检测教程”，而是因为它能够让我们在掌握普通目标检测的同时，再多学一层更一般、更通用的目标检测方法。

同时，OBB 的难度又比较合适。它比普通水平框检测更进一步，会让我们真正理解边界框表示、检测头、标签分配和 Loss 等关键问题；但它仍然属于二维目标检测的范畴，不会像 3D 检测、实例分割或者视觉大模型那样，一下子把问题扩展到完全不同的技术体系。

因此，我选择了OBB 作为《万物·炼器》这条学习路线最终选择的参考任务。

### 4.2 不仅仅是 YOLO11 OBB


这个项目不是：

> **“从零复刻一份 YOLO11 源码。”**

更不是逐行照抄 Ultralytics 的实现。

整个学习过程大致分成两个阶段。

### 第一阶段：先自己造

前面的内容会尽量暂时放下成熟目标检测框架，从最基础的内容开始。

例如：

- 神经元；
- Tensor；
- 计算图；
- Conv2d；
- 反向传播；
- Backbone；
- 多尺度特征；
- P3 / P4 / P5；
- FPN；
- 检测头。

先自己搭出一个能够工作的简化目标检测网络。

这样做的目的，是先真正理解一个检测器的基本骨架。

### 第二阶段：再去看高手怎么造

当我们已经能够理解一个基本检测网络之后，再开始研究成熟模型。

从这里开始：

> **YOLO11 OBB 会成为本系列最重要的一张参考蓝图。**

我们会逐步研究其中的重要设计，例如：

- `Conv2d + BatchNorm2d + SiLU`
- Bottleneck
- C2f
- C3k2
- SPPF
- Backbone / Neck
- 多尺度特征融合
- Detection Head
- OBB Head
- DFL
- Task-Aligned Assigner
- ProbIoU
- Loss
- 训练与推理流程

但学习源码的目的并不是把官方代码搬过来。

我们更希望遵循这样的过程：

```text
自己遇到问题
    ↓
分析问题为什么会出现
    ↓
看看成熟模型怎么解决
    ↓
读懂它的设计
    ↓
理解为什么这样设计
    ↓
自己重新实现
    ↓
再尝试根据任务需要修改
```

所以，这个系列真正想学习的并不仅仅是 YOLO11 OBB 本身。

而是：

> **借鉴成熟目标检测模型的设计之道，从卷积神经网络和普通目标检测出发，一直走到 OBB，最终获得自己分析、修改和实现目标检测网络的能力。**

---

## 5. 这个系列有什么特点？

### 5.1 尽可能从零开始

默认读者：

- 会一些 Python；
- 知道什么是类和函数；
- 能看懂基本 PyTorch 代码；

但不要求已经系统学习过深度学习。

一些经常被教程一笔带过的内容，也会尽量重新解释。

例如：

- Tensor 到底是什么；
- 卷积层的参数从哪里来；
- 一个神经元怎样计算；
- 特征图是什么；
- 通道是什么；
- 下采样是什么；
- 计算图是什么；
- 梯度是什么；
- `loss.backward()` 到底发生了什么。

### 5.2 不满足于“会调用”

很多教程最终会变成：

```python
model = YOLO(...)
model.train(...)
```

这样当然方便。

但这个系列更感兴趣的是：

> **这些代码背后到底发生了什么？**

因此会自己实现大量核心模块，并通过：

- Tensor Shape；
- Forward；
- Backward；
- 梯度；
- Loss；
- 中间输出；
- 实验结果；

去验证自己的理解。

### 5.3 尽量说人话

数学公式无法完全避免。

但如果能够先用直觉解释清楚，就不会急着堆公式。

一个公式真正出现之前，我们会尽量先回答：

> **它到底想干什么？**

然后再去理解公式里的符号。

### 5.4 文章和代码一起推进

这里的代码不是单独准备的一堆 Demo，文章讲到哪里，代码就尽量实现到哪里。

整个项目更像是一个模型不断升级的过程：

```text
能跑
  ↓
能检测
  ↓
能多尺度检测
  ↓
结构更加稳定
  ↓
更加接近成熟检测器
  ↓
支持 OBB
  ↓
能够训练和推理
  ↓
能够针对实际任务继续修改
```

---

## 6. 修炼路线

整个系列会从一个非常简单的卷积网络开始，不断升级。

大致路线如下：

```text
Python / PyTorch 基础
        ↓
神经元与计算图
        ↓
Conv2d
        ↓
反向传播
        ↓
简单 Backbone
        ↓
多尺度特征 P3 / P4 / P5
        ↓
FPN
        ↓
Conv + BN + SiLU
        ↓
Bottleneck
        ↓
C2f / C3k2
        ↓
SPPF
        ↓
YOLO 风格 Backbone / Neck
        ↓
Detection Head
        ↓
DFL
        ↓
Task-Aligned Assigner
        ↓
ProbIoU
        ↓
OBB Head
        ↓
旋转目标检测
        ↓
完整训练与推理
        ↓
根据实际任务修改模型
```

我们会一点一点把最开始的“凡铁”，炼成一件真正能够工作的目标检测“法器”。

---

## 7. 适合哪些读者？

这个系列主要写给：

- 会一些 Python，但刚开始学习人工智能的人；
- 正在学习 PyTorch 的同学；
- 第一次接触目标检测的读者；
- 会使用 YOLO，但不知道 YOLO 内部发生了什么的人；
- 会训练 YOLO，但不知道如何修改网络的人；
- 看过很多目标检测教程，却始终觉得知识比较零散的人；
- 想系统理解 Backbone、Neck、Head、Loss 等模块的人；
- 想系统学习目标检测的人；
- 想学习如何阅读深度学习框架源码的人；
- 想根据自己的实际任务修改 YOLO 的工程人员或学生。

如果你已经能够熟练实现检测器、阅读大型深度学习框架源码，那么前面的内容可能会比较基础。

不过后面的 OBB、DFL、TAL、ProbIoU、Loss 和完整检测流程，仍然可以作为一套系统化的实现笔记。

---

## 8. 关于 AI

这个项目是在 **AI 辅助下完成的**。

AI 参与了包括但不限于：

- 技术资料整理；
- 源码辅助分析；
- 概念讨论；
- 文章结构优化；
- 表达方式修改；
- 代码检查；
- 实验思路讨论；
- Bug 排查；
- 技术问题交叉验证。

但这里并不是简单地让 AI：

> “生成一篇目标检测教程。”

整个系列仍然遵循这样的过程：

```text
阅读资料
    ↓
阅读源码
    ↓
理解原理
    ↓
自己实现
    ↓
运行实验
    ↓
发现问题
    ↓
继续查证
    ↓
撰写文章
    ↓
反复修改
```

AI 更像一个随时可以讨论问题的：

> **学习伙伴 + 代码审阅助手 + 技术讨论对象。**

同时也必须强调：

> **AI 会犯错。**

因此，文章中的重要技术结论会尽可能通过：

- 官方源码；
- 官方文档；
- 论文；
- 实际代码；
- 实验结果；

进行交叉验证。

即使如此，仍然不能保证所有内容绝对正确。

如果你发现文章或者代码存在问题，非常欢迎提交 Issue。

---

## 9. 关于“工业级”

副标题中使用了：

> **工业级旋转目标检测网络**

这里需要特别说明。

它并不是说：

> 本仓库当前所有教学代码都已经可以不经修改直接部署到工业生产环境。

这个系列所说的“工业级”，更多是指我们不希望最终停留在一个只能演示原理的玩具网络。

我们的目标是逐步学习和实现现代成熟目标检测器中真正使用的：

- 网络模块；
- Backbone；
- Neck；
- 多尺度特征融合；
- Detection Head；
- 标签分配；
- 边界框回归；
- Loss；
- DFL；
- OBB 表达；
- 训练流程；
- 推理流程；
- 模型配置；
- 工程组织方式。

最终，让这个从零搭建的模型逐渐接近一个完整现代目标检测器应有的结构。

---

## 10. 关于代码

为了便于学习，本项目中的教学代码通常优先考虑：

> **可读性 > 极致简洁 > 极致性能**

成熟框架中经常存在大量为了：

- 性能；
- 兼容性；
- 部署；
- 多任务支持；
- 不同硬件；
- 长期维护；

而设计的复杂工程代码。

这些内容并不一定适合第一次学习时全部展开。

因此，本项目会根据学习进度适当简化。

等到这些复杂设计真正变得必要时，再逐步引入。

所以：

> **本仓库中的教学实现不应被简单等同于 Ultralytics 官方实现。**

如果用于实际工程项目，请根据具体任务评估：

- 性能；
- 精度；
- 稳定性；
- 推理速度；
- 部署环境；
- 维护成本；
- 开源许可证要求。

---

## 11. 阅读建议

如果你是第一次学习目标检测，建议按照章节顺序阅读。

也不要看到副标题中的 `YOLO11 OBB`，就把这个系列理解成一套只讲旋转目标检测的教程。

前面相当长的一段路线，学习的都是卷积神经网络和普通目标检测同样需要掌握的核心知识。OBB 是在这些基础之上的进一步扩展。

因此，不要急着直接跳到 YOLO11 OBB。

因为前面很多看起来很基础的概念，都会在后面的 YOLO11 OBB 中反复出现。

如果这些基础真正理解了，后面阅读成熟模型源码时会轻松很多。

这个系列并不追求：

> **最快把 YOLO 跑起来。**

更希望做到：

> **一步一步把 YOLO 看懂。**

最终再进一步做到：

> **能够根据自己的任务修改 YOLO。**

---

## 13. 项目状态

> 🚧 **持续更新中**

---

## 14. 发现问题？

如果你发现：

- 技术错误；
- 代码 Bug；
- 表述不清；
- 某个地方对于初学者仍然太难；
- 有更好的解释方法；
- 某段代码无法运行；
- 实验无法复现；
- 某个结论值得进一步讨论；

欢迎提交 **Issue**。

尤其欢迎这样的反馈：

> **“这里我没看懂。”**

因为对于这个项目来说：

> **让一个初学者真正看懂，本身就是衡量文章是否写清楚的重要标准。**

---

## 15. 最后

学习深度学习有时候很像修仙小说中的法宝炼制。

所以这个项目想做的事情很简单：

> **亲手炼出属于自己的目标检测器。**

---

## 16. License

本项目中的原创文章、代码、图片、示意图及其他原创内容，其著作权归作者所有。

本项目公开在 GitHub 上，主要用于学习交流和内容展示。  
**公开访问并不代表授予复制、转载、修改、分发或使用许可。**

在未获得作者明确授权的情况下，不得以任何形式：

- 转载、复制或大段摘录本项目中的文章；
- 修改、整理后再次发布本项目内容；
- 将本项目文章、代码、图片或其他内容用于其他网站、公众号、课程、出版物或知识付费内容；
- 将本项目代码全部或部分用于其他项目、产品或商业应用；
- 对本项目内容进行二次分发、二次开发或重新包装发布；
- 以其他方式使用本项目中的原创内容。

如需转载、引用、修改、二次开发、项目使用或商业使用，请提前联系作者并取得明确授权。

个人可以在线阅读本项目内容，用于个人学习和研究；正常的少量引用应注明出处及本项目来源。

### 第三方项目与代码

本项目在学习和实现过程中参考了 YOLO11 OBB、Ultralytics 等公开项目的设计思想、文档和源码。

属于第三方项目的代码、商标、文档及其他内容，其版权和许可仍归原权利人所有，并遵循各自的开源许可证或使用条款。

本项目的上述授权声明仅适用于作者拥有版权的原创内容，不改变任何第三方项目原有的许可证要求。

未经授权，请勿转载、复制、修改、分发或用于其他项目。

---

## 17. Star & 交流

如果这个项目对你有所帮助，欢迎：

- ⭐ Star
- 提交 Issue
- 参与讨论
- 帮助纠错

也欢迎一起学习，一起拆模型，一起炼器。

